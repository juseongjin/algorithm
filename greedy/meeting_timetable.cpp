#include <bits/stdc++.h>

using namespace std;

int n;
vector<pair(int,int)> plan;

int main(){
    cin >> n;
    for(int i=0; i<n; i++){
        int start, end;
        cin >> plan.push_back({start, end});
    }
    // 줄 세우고, 앞에서 부터 간격 비교하면서 정해지면 end시간 다음으로 부터 또 비교~~
    return 0;
}